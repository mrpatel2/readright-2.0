{
  "name": "readright-story-proxy",
  "version": "1.0.0",
  "description": "Thin backend proxy for ReadRight's AI Story Builder. Holds the OpenAI key server-side so it never ships inside the Flutter app.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
